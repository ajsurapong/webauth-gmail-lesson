<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile</title>
</head>

<body>    
    <a href="/">Home</a>
    <% if(user) { %> 
    <a href="/auth/logout">Logout</a>
    <% } else { %>
    <a href="/auth/login">Login</a>
    <% } %>

    <h1>Profile</h1>
    <p>Welcome <%= user.email %></p>
    <img src="<%= user.photo %>" alt="profile photo" width="100">
</body>
</html>